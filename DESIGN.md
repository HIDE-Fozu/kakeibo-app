# 家計簿アプリ 設計・引き継ぎドキュメント（作成中）

> **⚠️ 2026-07-03: このファイルはヒアリング記録。正式版は `docs/superpowers/specs/2026-07-03-kakeibo-app-design.md`（敵対的レビューで堅牢化した修正版）に移行。以後はそちらを読む。** 本specでは date=civil date保存、カテゴリFK/未分類sentinel、インポートのアトミック化、パーサ堅牢化（インボイス番号/ポイント除外・小計vs合計・座標正準空間・和暦）、編集/削除フロー追加、オフライン再定義＋オンデバイス自動バックアップ、を追加。ユーザー確定待ち＝§14の4決定（オフライン再定義＆バックアップ／paymentMethod列は残しUIから外す／レシート画像は既定破棄／サマリ独立タブ）。

- ステータス: **spec清書済み・ユーザーレビュー待ち**（承認後 git init→commit→writing-plans）
- 最終更新: 2026-07-03
- 開発方針: 今は Windows 機のため**要件定義・設計のみ**。実装は後日 **Mac + Xcode** で着手。
- このドキュメントの置き場所: `C:\Users\wilsh\kakeibo-app\DESIGN.md`（実装プロジェクトのルート予定地）

---

## 0. 次回セッションの再開手順（ここから読む）

1. 本ドキュメントの「2. 確定した意思決定」を読めば前提は全部わかる。
2. 未了タスク＝**設計セクション⑤〜⑨をユーザーに提示** → **①〜④を含め承認を得る**。
   - オープン論点（下記「5. 未承認・オープンな論点」）を先に潰す。
3. 承認後、正式 spec を `docs/superpowers/specs/2026-07-02-kakeibo-app-design.md` 相当に清書 → git 初期化 → コミット。
4. その後 `writing-plans` スキルで Mac 着手用の実装計画を作成（ブレストの終端）。
5. 実装は Mac へ移ってから。着手前に「7. 実装前の宿題」を済ませる（特に実レシートのサンプル収集）。

---

## 1. 概要（何を作るか）

スマホ完結の個人向け家計簿アプリ。**Flutter / iOS 先行**。

コア体験:
- **カレンダー起点**：カレンダーからその日の入力内容を確認 → 入力。
- **レシートOCR**：レシートを撮影し金額・日付を自動抽出、**確認画面で修正して保存**。
- 収入・支出の両方を記録し、月次で集計。
- **完全オフライン**（財務データを一切サーバに出さない）。バックアップは手動エクスポート/インポート。

当初要件にあった「カード明細スクショからの自動入力」は、二重計上（同じ支出をレシートとカード明細で二重登録）の照合が運用上重くYAGNIと判断し、**v1では外した**（後述の非対象）。

---

## 2. 確定した意思決定（ヒアリング結果）

| # | 論点 | 決定 |
|---|------|------|
| 1 | 「スマホ完結」の意味 | **データを外に出さない＝完全オフライン**（クラウド/LLM/Cloud Vision 不使用） |
| 2 | 対象OS | **iOS のみ**（まず iPhone で完成させる） |
| 3 | ビルド環境 | Mac あり（実機ビルド可）。ただし**今は Windows なので設計まで**。ロジックは Windows の `flutter test` で検証、実機/カメラ/OCR部は Mac |
| 4 | カード明細取込 | **v1では外す**。カード支出は手入力。将来やるなら「スクショOCR」より「会員サイトの明細CSV取込」が確実（役割分担: レシート=現金 / CSV=カード で二重計上回避）とメモ |
| 5 | レシートOCRの抽出項目 | **金額＋日付のみ**（堅牢重視）。店名・カテゴリは確認画面で手入力 |
| 6 | 収支の範囲 | **支出＋収入の両方**（標準的な家計簿） |
| 7 | バックアップ方針 | **手動エクスポート/インポート（JSON＋CSV）**、共有シート経由（iCloud Drive/メール等） |
| 8 | ローカルDB | **drift（SQLite・型付きクエリ）** |
| 9 | OCRエンジン | **Apple Vision（Swift プラットフォームチャネル）**。日本語精度が高くモデル追加DL不要 |

### 技術スタック（確定）
- 状態管理: **Riverpod**
- カレンダーUI: **`table_calendar`**
- 画像取得: **`image_picker`**（カメラ＋ギャラリー）
- 通貨: **日本円のみ**（多通貨は非対象）
- OCR抽象化: **`OcrService` インターフェース**（裏に AppleVision実装、将来 ML Kit も差し替え可）
- 金額保持: **円の整数**（浮動小数の誤差回避）／日付は**日単位**（時刻なし）

---

## 3. 提示済みの設計（①〜④・ユーザー承認待ち）

### ① v1スコープ
- **入れる**: カレンダー閲覧・入力／手入力（収入・支出）／レシートOCR（金額＋日付、確認画面あり）／月次サマリ／手動バックアップ（JSON/CSV）。
- **非対象（明示）**: カード明細のスクショ/CSV取込・二重計上照合／クラウド同期／予算アラート／固定費・繰り返し登録／多通貨／複数口座・資産管理／ウィジェット・通知。※すべて後日Phaseで追加可能な形にはする。

### ② データモデル
**Transaction（取引）**
- `id`, `type`(expense/income), `amount`(整数・円), `date`(日付), `categoryId`(FK), `paymentMethod`(enum・任意, 既定=現金), `memo`(自由記述=店名やメモ), `source`(manual/receiptOcr), `createdAt`, `updatedAt`

**Category（カテゴリ）**
- `id`, `name`, `type`(expense/income＝カテゴリは収支別), `icon`(絵文字/アイコンキー), `sortOrder`, `isArchived`
- プリセット初期投入＋ユーザーが追加・改名・並べ替え・アーカイブ可。
- プリセット案（支出）: 食費／外食／日用品／水道光熱費／通信費／交通費／交際費／趣味・娯楽／衣服・美容／医療・健康／住居／教育／特別費／その他
- プリセット案（収入）: 給与／賞与／副収入／その他

**PaymentMethod（支払方法）**: v1は enum（現金／クレジットカード／電子マネー／口座引落／その他）。個別カード名はテーブル化で将来拡張可。**この項目は不要なら削れる**（カード取込を落としたので重要度低）。

### ③ 画面構成とナビゲーション
下タブ3つ ＋ 入力FAB:
1. **カレンダー（ホーム）**: `table_calendar` 月表示。各日セルに支出合計（＋収入あれば差引）。月ヘッダに月次合計（支出・収入・差引）。日タップ→下部にその日の取引リスト。
2. **サマリ**: 選択月の支出/収入/差引 ＋ カテゴリ別内訳（ドーナツ or 棒）。
3. **設定**: エクスポート／インポート、カテゴリ編集。
- **入力FAB**: 「手入力」or「レシートから」。
- **EntryForm（入力フォーム）は1つを使い回す**（手入力もレシート確認も同一。違いは初期値プリフィルだけ）。項目＝収入/支出トグル・金額・日付・カテゴリ・支払方法・メモ。

### ④ データフロー（3経路）
- **手入力**: FAB→EntryForm（空）→保存→カレンダー更新。
- **レシートOCR**: FAB→カメラ/ギャラリー→`OcrService`が文字＋座標を返す→`ReceiptParser`が金額・日付抽出→**EntryFormにプリフィル（自動抽出欄はハイライト＝要確認）**→ユーザーが店名・カテゴリ補完・修正→保存。抽出失敗時は空フォームにフォールバック（金額欄フォーカス）。
- **バックアップ**: 設定→エクスポートで全取引＋カテゴリをJSON（正確な復元用）/CSV（表計算閲覧用）に書き出し→共有シート。インポートは**取込前に自動退避エクスポート**→置換（確認あり）。

---

## 4. 次回提示予定の設計メモ（⑤〜⑨・下書き済み）

> 未確定。次回ユーザーに提示して承認を得る。ここに書いてあるのは提案の叩き台。

### ⑤ OCR抽出ロジック（`ReceiptParser`・純Dart）
- 入力: 正規化済みOCR結果＝`List<TextBlock{ text, boundingRect }>`（Apple Vision / ML Kit 出力の抽象化）。
- **金額抽出**:
  - 金額候補を正規表現で収集（`¥1,234` / `1,234円` / `1234` 等）。
  - 「合計／小計／お買上げ／計」等キーワード行の**同一行右側・近傍**の金額を優先。
  - キーワードが無ければ金額候補の**最大値**を合計とみなす（フォールバック）。ただし「お預り／お釣り／現金」に紐づく額は除外。
  - 税込を採用。
- **日付抽出**:
  - 正規表現で `YYYY年MM月DD日` / `YYYY/MM/DD` / `YY.MM.DD` / 令和表記等をマッチ。複数あれば最上部（発行日）優先。
  - 見つからなければ「今日」を既定にして確認画面で修正。
- 各抽出値に**確信度**を持たせ、確認画面で色分けハイライト。

### ⑥ アーキテクチャ / レイヤリング（案）
```
lib/
  app/        … ルーティング, テーマ, main
  data/       … drift DB(テーブル/DAO), モデル, リポジトリ(Transaction/Category), backup(BackupService: JSON/CSV)
  domain/
    services/ … OcrService(抽象) + AppleVisionOcrService(プラットフォームチャネル実装),
                ReceiptParser(純Dart)
  features/   … calendar/ entry/ receipt/ summary/ settings/（画面 + Riverpod providers）
ios/          … Swift プラットフォームチャネル（Apple Vision: VNRecognizeTextRequest）
```
- リポジトリでDBを隠蔽。UIはRiverpod provider経由。OCRエンジンは`OcrService`の裏に隔離し差し替え可能。

### ⑦ テスト戦略（ユーザー方針「AI自身がヘッドレス検証」に対応）
- **Windows / `flutter test` で回せる（AIが自走検証）**:
  - `ReceiptParser`（実レシートから録ったOCR結果フィクスチャを食わせて抽出精度を検証）
  - リポジトリ/DAO・集計クエリ（driftはVM上のsqlite3でテスト可＝Windows可）
  - `BackupService` の往復（export→import→一致）
  - カテゴリ/プロバイダのロジック（フェイク使用）
- **Mac必須（実機/シミュレータ）**: Apple Vision の実精度・カメラ・image_picker・レシート一気通貫フロー。
- **橋渡し**: デバッグ用の「OCRフィクスチャ記録ツール」を用意し、実機のApple Vision出力をJSONで保存→そのJSONをWindowsの単体テストに投入。→ iOS依存を避けてロジック検証を自動化。

### ⑧ エラー処理・エッジケース（案）
- OCRが空/低確信 → EntryFormを空で開き注記表示（金額欄フォーカス）。
- 合計が複数/曖昧 → ベスト推定を採用しハイライトで要確認。
- 金額パース不能 → 空欄で保存させない（バリデーション）。
- 未来日/非現実的な日付 → 今日を既定。
- インポート: エクスポートファイルに**スキーマバージョン**を持たせ検証。v1は「置換（全消し→投入）＋取込前自動退避」で単純化。
- カテゴリ削除でそのカテゴリの取引がある場合 → ハード削除せず**アーカイブ**、または「未分類」へ再割当。
- 空状態（取引ゼロ）のUI。

### ⑨ 実装前の前提・宿題（重要）
- **実レシートのサンプル画像を多様に収集**（スーパー/コンビニ/ドラッグストア/飲食 等 十数枚）→ `ReceiptParser` のヒューリスティクス調整＆フィクスチャ化。※これが無いと抽出精度を詰められない。
- **Apple Vision 出力フォーマットの実機確認**（Mac）。
- **Apple Developer アカウント**（実機/TestFlight配布用）。
- Mac + Xcode + Flutter iOS toolchain の用意。

---

## 5. 未承認・オープンな論点（次回で決める）
- 設計①〜④ 全体の**承認**（まだ得ていない）。
- **②の `paymentMethod` フィールドを残すか削るか**（カード取込を落としたので任意）。
- **③のタブ構成**：サマリを独立タブにするか、カレンダー画面内に畳むか。
- ⑤〜⑨ の中身への合意。

---

## 6. 参考・背景メモ
- 完全オフライン確定のため、カード明細の任意レイアウト構造化（本来LLM Visionが得意）は不採用。→ カード取込自体をv1で削除。
- 将来カード取込を復活させるなら **スクショOCRより会員サイト明細CSV** が確実（楽天/セゾン/ビューはCSV DL可）。ユーザー保有カード＝**楽天・セゾン・ビュー**の3枚（PayPay・モバイルSuicaは先々）。
- EntryFormを手入力/レシート確認で共有する設計、ReceiptParserを純Dartで独立させる設計が、単純化とテスト容易性の要。
