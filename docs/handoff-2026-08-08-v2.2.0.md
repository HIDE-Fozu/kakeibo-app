# 引き継ぎ 2026-08-08〜11 — v2.2.0「つきいち管理合体」＋UI磨き込み

前の正典: docs/handoff-2026-08-05-v2.1.0.md（毎月の固定費・収入 提出直前）。
本書は v2.2.0 の全変更・設計の要点・残作業をまとめた現時点の正典。

## 🎯 次セッションの入り口（2026-08-16b セッション終了時点）

- **案B「ボトムアップ内訳」実装済み = `feature/split-bottomup`**（審査提出済みの
  tsukiichi-merge 568fc3a から分岐。46は審査待ちのまま・このブランチは次リリース用）。
  **539テスト緑 / analyze 0**。
- **同日FB⑩: 分割払いの管理 = 毎月タブに「分割払い」セクション（schema v10）** —
  「登録後に編集する方法がない」FB。計画を実体化して編集/削除できるようにした。
  - **schema v10**: `installment_plans` テーブル新設＋ `transactions.installment_plan_id`
    （nullable・計画へFK cascade）。migration test は v9→v10 を追加し、
    既存の v7/v8 最小DBテストにも transactions 定義を追加（v10が列追加で触るため）。
  - **backup formatVersion 8**: installmentPlans 配列＋取引の installmentPlanId を
    同梱・検証（計画IDに解決できない取引は拒否）。v7以前は空/nullで復元。
  - リポジトリ `DriftInstallmentPlanRepository`: add/replace/delete を1トランザクションで
    （replace=紐づく取引を全削除して作り直し・delete=取引ごと削除）。
  - UI: 毎月タブ「固定費・収入」と「つきいちタスク」の間に「分割払い」セクション
    （hub-installment-add・行=カード名/月々×回数・開始月〜/総額・タップで編集）。
    InstallmentPage は plan 非null で編集モード（プリフィル・開始は月ドロップダウン
    ±18ヶ月・AppBarに削除→確認ダイアログ）。
  - ⚠️ このv10より前（同日の初版 15363a5）で登録した分割払いは計画に紐づいて
    いないため管理対象外（取引としては残る。消すなら手で削除して登録し直す）。
  - ★テストの罠（再発防止）: widgetテストでのstream await は tester.runAsync で包む/
    ルートページの pop は Navigator.push で開いてから（'_history.isNotEmpty' assert）。
- **同日FB⑦: 分割払いの登録（機能のみ・固定費への本統合は後日）** —
  例「33,000円を10回払い・実質年率17%」＋支払日/開始（今月/来月・既定=来月）→
  N ヶ月分の支出取引を一括登録（memo=「分割払い i/N回」・store=カード名・source=manual）。
  - 計算 = `domain/services/installment_calc.dart`: 実質年率の元利均等
    （月額 = P·r/(1−(1+r)^−n)）・総額を円丸め・**端数は初回**・0%/1回は手数料なし。
    15%・10回→手数料7.0円/100円でカード各社の表とほぼ一致（厳密には率で調整してもらう）。
  - UI = `recurring/presentation/installment_page.dart`（毎月タブ・固定費一覧の下の
    「分割払い」ボタン hub-installment-add から）。プレビュー=月々/初回/手数料/総額。
  - **カード名称（任意）を入れて保存すると名称＋年率を端末に記憶**
    （SharedPreferences `installmentCards`・"名称\t率" の StringList・
    settings_controller の InstallmentCard/saveInstallmentCard）。次回から
    「登録済みカード」ドロップダウンで選ぶと年率が入る。
    ※DBテーブル化・バックアップ同梱は今後の宿題（今は端末ローカルのみ）。
  - l10n 14キー×9ロケール（installment*）。テスト = installment_calc_test（ゴールデン値）
    ＋ installment_page_test（10取引の日付/金額/メモ・カード保存/選択）。
  - ★integration shotsの罠: 実機はキーボードでビューポートが縮み ListView が
    遅延構築 → ensureVisible が「No element」。ドラッグでスクロールする。
- **同日FB⑥/⑧/⑨は 0ce937f 参照**（固定費の終了月・カレンダー凡例撤去・
  合計入力フェーズの磨き込み）。目視 = fb_1〜4.png / tf_1〜4.png。
- **同日FB⑤（最終形）: 金額0の「カテゴリを追加」は「まず合計値の入力」から開始** —
  ボトムアップ（品目の総和が合計）は「合計値しか入力できない状態から始めたい」で置き換え。
  - `splitTotalPending` を EntryFormState に追加。金額0で startSplit すると true。
    この間、電卓は合計(amountYen)へ配線（演算子列なし）・上部の金額表示をアクティブ枠で
    ハイライト・行/帯/＋品目はコントローラ側ガード（_leaveTotalPendingIfAble）。
  - 合計0の間は行と帯をブロック＆ディム＋ヒント「まず合計を入力してください」。
    合計が入るとディム解除＋「つづけて品目の行をタップしてください」→ 行に触れた時点で
    フェーズ解除 → 以後は従来のトップダウン（残り行）。やめる/金額>0開始ではフェーズなし。
  - **splitBottomUp / displayAmountYen は撤去**（挙動はトップダウンのみに戻った）。
    l10n: entrySplitEnterTotalFirst / entrySplitTapRowToStart 追加（9ロケール）。
  - テスト: split_total_first_test.dart（旧 split_bottomup_test を置換）・コントローラ2本
    書き換え・539緑。目視 = split_total_first_shots_test.dart → tf_1〜4
    （フェーズ中ディム→合計入力→解除→残り520まで確認）。
  - ★shots/テストの罠（再発）: 行中央のタップはメモボタンに当たる → 番号バッジ
    （split-lineno-N）を狙う。
- 挙動: **金額0で「カテゴリを追加」→ そのまま内訳に入り、ヘッダの金額表示が
  品目の総和で追従する**（`displayAmountYen`）。末尾の行は**従来どおり「残り」**
  （総和＝合計なので常に¥0・超過にならない。カテゴリ枠・行番号も従来どおり）。
  保存は従来どおり品目ごとの取引グループ（総額＝品目の総和）。
  - **2026-08-16 FB: 初版は末尾を「合計 ¥N」行にしたが「合計が重複しているので却下」**
    （ヘッダと同じ数字が2回出る）→ 合計はヘッダに一本化し「これまでと同様に残りを表示」に。
  - モードは**導出**: `splitBottomUp = splits != null && amountYen == 0`（スキーマ変更なし。
    内訳中は電卓が品目行を編集し amountYen は変わらない。保存済みグループの開き直しは
    総和>0なので常にトップダウン）。
  - ＋品目・末尾行アクティブ時の打鍵→新行で受ける、は従来の仕組みがそのまま効く。
    末尾行のカテゴリは残額0なので付けても品目にならない（トップダウンの「残り¥0」と同じ）。
  - 検証: 総和>0＋金額あり行は全てカテゴリあり。トップダウンの「sum==合計」照合が
    無いぶん、**式が不正な行（÷0等）は `_splitsValid` で直接弾く**。
  - **案Aのゲート（金額0でグレー＋SnackBar）は撤去**。`entrySplitNeedsAmountSnack` を
    9ロケールから削除（初版で足した `splitTotalLabel` はFBで撤回済み・追加キーなし）。
  - 一括内訳（レシート品目あり）は総額前提なので従来どおり金額>0のときだけ。
- **同日FB②: 通常入力のメモも「セル内」へ** = 品目行（single-item-row）に内訳の行と
  同じメモボタン（白ピル→ダイアログ・`entry-memo-btn`）を追加し、電卓下の独立した
  「詳細メモ」欄（memo-field）を**廃止**。部品は `MemoPillButton` / `SplitMemoDialog`
  として split_entry_panel.dart に共通化（内訳の行も同じ部品に置換済み）。
  編集モード・レシート確認も同経路（既存メモは行内に本文表示）。
  一括内訳のグループメモ欄（カテゴリ見出しの右）は従来どおり。
  テスト: entry_memo_chip_test.dart 新規・entry_screen_test 2本を新配置に更新・
  keyboard_done_bar_test は店舗名欄で検証するよう変更。
- **同日FB③: 「品目2にメモが出ない」= 残額行にもメモボタン** — 残額行は
  「カテゴリを付ければ最後の1品になる」行なのにメモが持てなかった。入力行と同じ
  MemoPillButton を追加（キーは連番の split-memo-btn-N）。メモピルは幅不足時に
  文言を省略してオーバーフローしない（Flexible+ellipsis）。
  ★既存テスト3本が「残額行の中央タップ」をしていてメモボタンに当たるようになった
  → split-tail-label（「残り」表示）を狙う形に更新（split_entry_test 2本・
  category_add_button_test）。挙動自体は入力行と同じで正しい。
- **同日FB④: 「カテゴリを追加で店舗名が小さくなる」** — 内訳パネルの店名欄を
  通常入力と同一の見た目に（labelText＋OutlineInputBorder＋isDense。縮小スタイル
  fontSize14/Underline/アイコンは撤去。右の「やめる」だけ残る）。
- 触ったもの: entry_form_controller.dart（splitBottomUp / displayAmountYen /
  _splitsValid / saveHint / startSplit のガード撤去）・entry_screen.dart（ゲート撤去・
  金額表示）・split_entry_panel.dart（残り=ボトムアップでは常に0・`split-tail-label` キー追加）・l10n 9arb＋gen。
  テスト: test/ui/split_bottomup_test.dart（旧 split_gate_test.dart を置換）・
  entry_form_controller_test.dart に3本（編集モードガード/保存フロー/saveHint・不正式）。
- 目視: `integration_test/split_bottomup_shots_test.dart` → build/qa_screens/bu_1〜5.png
  （bu_2=残り¥0・bu_4=2品目でヘッダ¥600・bu_5=トップダウン比較で退行なし）。
  実機TI10B1へ profile ビルドを devicectl で配備・起動済み（installは正常・データ保持）。
- **残り**: ユーザーの操作感確認（シミュレータ/実機）→ FB反映 → 次リリースへ
  （バージョンは未bump。次build=2.2.0(47)か2.3.0かは提出時に判断）。
  そのほかの積み残しは次節（入力画面の抜本リデザイン・審査結果対応）を参照。

## 🎯 次セッションの入り口（2026-08-16 セッション終了時点）

- **v2.2（ビルド 2.2.0(46)）を審査提出済み（2026-08-16・審査待ち・承認後自動リリース）**。
  ASCのWhat's New 9ロケールも入力済み。審査結果はメールで届く。
  リジェクトされたら本書の経緯（build 41〜46）を読んでから対応。
- **HEAD = 8e22492（build 46・feature/tsukiichi-merge・未push）**。全コミット済み・536テスト緑。
- **次の課題（優先順）**:
  1. **案B**: 金額0で「カテゴリを追加」→ ボトムアップ内訳（残り行が合計行になる）。
     案A（ゲート＋SnackBar）は46に入っている。設計論点は「2026-08-15 セッション」の壁打ち参照
  2. 審査通過後: main へのマージ・push の承認をユーザーに確認
  3. 積み残し（2026-08-14節より）: 入力画面の抜本リデザイン（合計値の置き場所が未決・モックURLあり）
- 実機TI10B1は TestFlight/App Store 由来の install coordination があるため
  profile 配備が失敗することがある（勝手に uninstall しない。データが消える）。

## 🎯 次セッションの入り口（2026-08-15c セッション終了時点）

- **現在地**: `feature/tsukiichi-merge`・**version 2.2.0+46**・536テスト緑 / analyze 0。
- **build 46 = 内訳の店名欄のラベル統一**: 内訳モードだけ「店名」だったのを
  通常入力と同じ「店舗名」（収入なら「会社名」）に。splitStoreNameHint キーは
  9ロケールから削除し entryStoreNameLabel / entryCompanyNameLabel を共用。
- **build 45 = 設定の色を「色」1機能に統合**（モック承認済み:
  https://claude.ai/code/artifact/13197b86-bbf0-4b0b-9616-577b02f2c4eb ）。
  - 「背景色」「テーマ色」2タイル → **「色」1タイル**（theme-color-tile）。
    シート = プリセット10色（ブルー=既定バッジ・グリーン〜ネイビー）＋カスタム
    （SV正方形＋色相バー。RGBスライダ廃止）。**タップ即ライブプレビュー**、
    適用以外で閉じたら元へ戻す。実装 = theme_color_sheet.dart（color_picker_dialog.dart は削除）。
  - **1色→全パレット導出 `derivePalette`**（theme.dart）: 塗り=白文字3.5:1まで締め、
    文字枠=白地4.5:1、選択面L92/罫線L87/背景L97/クロームL94の同系ティント。
    既定(null)は現行トークンとバイト一致（テストで固定）。
  - **`KakeiboPalette` ThemeExtension 新設**。主色まわりの定数直書きを
    `context.kakeiboPalette` 参照に置換（カレンダーのリング・内訳＋ボタン・
    サブカテゴリ帯・下部タブのカレンダーアイコン）→ カスタム色が全画面に追従。
  - 設定は `themeColor` 1キー。**null=キー削除**（値を焼き込まない）。
    旧 pageColor/accentColor キーは起動時に無条件削除（ユーザー確認済み: 旧色設定の利用者なし）。
  - l10n: settingsColor* 16キー×9ロケール追加・旧5キー削除。
  - 文言は社会人向け（プリセット/カスタム/適用。ひらがな幼児語は不可＝ユーザーFB）。
- 目視: integration_test/theme_sheet_shots_test.dart → ts_1〜6（シート・グリーン・ローズ確認済み）。
- **ストアに出すのはこの46**（43〜45はスキップ可）。

## 🎯 次セッションの入り口（2026-08-15b セッション終了時点）

- **現在地**: `feature/tsukiichi-merge`・**version 2.2.0+44**・533テスト緑 / analyze 0。
- **build 44 = 旧既定色の移行**。実機で「青が反映されない」FB → 原因は設定の
  テーマ色/背景色に**旧既定の緑が保存されていて新既定より勝っていた**
  （ピッカーの「既定に戻す」は当時の既定値をそのまま保存する仕様のため）。
  `AppSettings.build()` で旧既定（accent: 1E6B5A/2F8570/287F73/2F7A6A・
  page: F6F5F0/FFFFFF/FFFCF7）と一致する保存値は remove して未設定に移行。
  カスタム色（旧既定と不一致）は尊重。テスト追加（core_providers_test）。
  即応の手当て: 実機の 設定→テーマ色→「既定に戻す」→決定 でも直る。
- ASC作業はMacのChromeで途中まで: 拡張は接続済みだが**ASC未ログインで停止**。
  ログイン後にやること=TestFlightビルド状態確認→2.2.0のバージョン作成→
  What's New 9ロケール→ビルド紐付け（審査提出はユーザー確認後）。

## 🎯 次セッションの入り口（2026-08-15 セッション終了時点）

- **現在地**: `feature/tsukiichi-merge`・**version 2.2.0+43**・532テスト緑 / analyze 0。
- **2026-08-15 = 主色をブルーへ＋バグ2件**（ユーザーFB起点）:
  1. **主色を「締まったブルー」#4A7FB5 に変更**。4案（ソフト#7A98C4/締まった#4A7FB5/
     スモーキー#6E8CA0/ティール#4FA3B8）を実ビルド撮影で比較するArtifactを作り、ユーザーが②を選定
     （https://claude.ai/code/artifact/9084c4de-216b-478f-9cc8-9eb6ba9b76f0）。
     トークン: kPrimaryFill=#4A7FB5（旧kMint改名）/ kPrimary=#2F5C8A / kPrimarySoft=#DDE8F4 /
     kLine=#D9E1EA / kSubScale青系。kNav*も kNavCalendar/kNavMonthly/kNavSummary/kNavSettings に改名。
     背景アイボリー・支出コーラル・収入グリーン・アプリコット/コーラルのアクセントは据え置き。
     **保存の白文字は 4.2:1 に改善**（緑時代の2.4:1問題はブルー選定で解消）。
  2. **「毎月の費用」が金額0で無反応に見えるバグ**: 予告帯の `amountYen > 0` 条件を外した。
     押した瞬間に「毎月N日に自動で記帳します」帯が出る。
  3. **「カテゴリを追加」（内訳）が金額0で無反応のバグ → 案Aで対応**: 金額0の間はボタンをグレー
     （onSurfaceVariant）にし、押されたらSnackBar「先に金額を入力してください」
     （entrySplitNeedsAmountSnack・9ロケール）。テスト2本追加（test/ui/split_gate_test.dart）。
- **★ユーザーと合意した進め方: まずこの案AでApp Storeバージョンアップ → その後、案B
  「金額0で内訳に入ったら残り行が合計行になるボトムアップモード」に着手**（Bは未着手）。
- **蛍光色すぎるFB（カテゴリ選択時の色）は青への切り替えで色ごと入れ替わったので、実機で再確認してもらう**。
- TestFlight: build 42はTransporter配信済みだがASC反映待ちの状態で青に切り替えた。**次にアップするのは43**。
  42が「処理中」のまま残っていても、43を上げれば良い。
- 目視: `integration_test/palette_shots_test.dart` → build/qa_screens/pal_*.png 11枚（青で撮影済み）。

## 🎯 次セッションの入り口（2026-08-14b セッション終了時点）

- **現在地**: `feature/tsukiichi-merge`・**version 2.2.0+42**・530テスト緑 / analyze 0。
- **やったこと = デザインシート「落ち着き × 親しみやすさ 改善案」のパレットへ全面差し替え**。
  前日（build 40）に入れた明るいミント系パレットを、指定の落ち着いた緑＋暖色アクセントに置換。
  | 役割 | 旧(build40) | 新(build41) |
  |---|---|---|
  | 全体背景 | #FFFCF7 | **#FAF8F4** ウォームアイボリー |
  | 主色（塗り・CTA・選択） | #6EDCC7 | **#6FB6A3** メイングリーン |
  | 主色（文字・枠） | #287F73 | **#2F7A6A**（派生。白地5.1:1） |
  | ソフト面 | #DDF7F0 | **#DCEFE8** ソフトミント |
  | 本文 / 補助 | #343A3A / #8B918F | **#2F3A3D / #748089** |
  | 罫線 | #E9E6E1 | **#D9E3DF** |
  | 支出 / 収入 | #F07878 / #54C69A | **#D97C6C / #59A98A** |
  | アクセント | Accent4色 | **アプリコット #F2B885 / コーラル #E58D7C / 注意 #D3A857 / 情報 #7A98C4** |
  - 追加トークン: `kApricot` `kCoral` `kAttention` `kInfo` `kCardShadow`(0 6px 18px rgba(47,58,61,.08))
    `kRadius`(16) `kSpace`(8)。CardTheme を角丸16・elevation 0 に。
  - 下部タブ: カレンダー=グリーン / 毎月=アプリコット / サマリ=コーラル / **設定=グレー**（モックに合わせた。
    以前は Accent Blue）。
  - カレンダー: 選択日=メイングリーン塗り＋白数字 / 今日=同色の輪郭（旧インディゴ #5C6BC0 を撤去）。
    家事ドット: やった=収入グリーン / 期日=注意アンバー / 超過=支出コーラル。
  - 内訳の品目行を**白**に（シートの「ホワイト＝カード・入力面」）。アクティブ行だけソフトミント。
    ＋品目ボタンをメイングリーンの丸に。
- **★仕様として飲んだトレードオフ（実機で気になったら戻せる）**:
  - **CTA（保存・FAB・＋丸ボタン）はモックどおり「メイングリーン塗り＋白文字」。白は 2.4:1** で
    本文基準は満たさない。読みづらければ `theme.dart` の `filledButtonTheme` / `floatingActionButtonTheme`
    の `foregroundColor` を `kInk` に戻すだけで 4.9:1 になる（build 40 まではこれだった）。
  - 一覧の金額（支出 #D97C6C）は背景上 **3.0:1**。締めるなら kWarnMuted(#C4685A・3.9:1) を流用可。
  - カテゴリのイラストPNGは円の下地色 #F5F8F5 が焼き込まれているので `kIconCircle` は据え置き。
- **2026-08-15 追加FB対応 = build 42**: 内訳の「残り」行も**普段は白**に
  （緑はアクティブ行だけ、という一貫したルールに揃えた。超過時だけ注意色を残す）。
- **目視確認**: `flutter drive --driver=test_driver/integration_test.dart
  --target=integration_test/palette_shots_test.dart -d <sim>` → `build/qa_screens/pal_*.png` 11枚。
  シミュレータ(iPhone 17)で全画面確認済み。**実機は未確認**。
- **次にやること**: ①実機で色の当たりを見る ②下の「2026-08-14」節に書いた**入力画面の抜本リデザイン**が
  そのまま残っている（モックURL健在・合計値の置き場所が未決）。

## 🎯 次セッションの入り口（2026-08-14 セッション終了時点）

- **現在地**: `feature/tsukiichi-merge`・**version 2.2.0+40**・530テスト緑 / analyze 0。
  実機TI10B1に build 40（profile）配備済。**ここまで全部このコミットに入っている**。
- **次にやること = 入力画面の抜本リデザインの続き**。実装はまだ1行も入れていない。
  **モック（触れる）= https://claude.ai/code/artifact/381cab2c-1069-4402-9940-026f9f8bf0f4**
  ソースは scratchpad なのでセッション限り。**再開時は上のURLを開いて現物を見ること**。
  確定済みの並び:
  ```
  日付（AppBarのタイトル位置）
  1 カテゴリ未選択                       ¥800   ← 元の日付行の位置
  🔁 毎月の費用              ＋ カテゴリを追加
  カテゴリ                        [収入|支出]   ← 支出を画面端側に
  [ タイル 4×2 ]
  [ 電卓 ]
  店舗名
  詳細メモ
  [保存]
  ```
  - 支出が端側なのは「端は誤タップが起きるが、当たっても既に選ばれている支出を
    選び直すだけで実害が無い」ため（収入が端だと誤タップで種別が変わる）。
- **★止まっている論点 = 合計値の置き場所**。ユーザー要望は「合計値はそのまま
  表示しておきたい」。ただし品目行の右端は各行の額に使うので競合する。候補3つ:
  ①日付タイトルの右（AppBar内・一等地だが日付と競合）②品目行の上に細い合計行
  （分かりやすいが縦を1行食う）③「カテゴリ」見出し行の左に小さく（縦を食わないが小さい）。
  **ユーザーも「バランスが悩ましい」と言って保留にした。ここから再開**。
- **未対応で残っているもの**（前セッションからの持ち越し）:
  - 一覧の金額 Expense #F07878 が背景上 **2.68:1**（本文基準4.5未満）。締めるなら #D45C5C(3.74)
  - 内訳のカテゴリ帯側にも「完了」ボタンが要るか（グリッド側だけ実装済み）
  - ボタン名「カテゴリを追加」（元は「複数のカテゴリを選択」）でよいか
  - 純白背景にしたので「入力欄が背景に同化する」(2026-08-09FB)の再発有無を実機で確認
  - 収入3カテゴリ（給与/賞与/副収入）のイラストはシートに絵柄が無く余りを充当した
- **反復ループ・罠は下の「2026-08-11 セッション終了時点」節がそのまま使える**
  （drive のハングと kill→再実行、sim は iPhone 17 / iOS 26.4、uninstall してから drive）。
  追加の罠: **`flutter drive ... | tail` はパイプのせいで完了まで何も出ない**ので
  `nohup ... > log 2>&1 &` にしてログを grep で待つこと。

## 🎯 前セッションの入り口（2026-08-11 セッション終了時点）

- **現在地**: ブランチ `feature/tsukiichi-merge` HEAD=`69ecd03`・**version 2.2.0+35**・
  **530テスト緑 / analyze 0 / untranslated 0**・ipa(35)検証済・実機TI10B1に配備済。
  作業ツリーはクリーン（`docs/asc-store-listings-2.0.0.md` の空行3行だけ以前から未コミット）。
- **次にやること = ユーザーが挙げる内訳(複数カテゴリ)まわりのUI微修正**。
  「もう少し修正したい点がある」とのことで内容は未提示。**まず要望を聞いてから着手**。
  直近3日は内訳画面を集中的に磨いており、関連ファイルは
  `lib/features/entry/presentation/{split_entry_panel,split_category_strip,entry_screen}.dart`。
- **速い反復ループ（このセッションで確立・そのまま使える）**:
  1. 実装 → `flutter test`（全530・約1分） → `flutter analyze`
  2. 見た目確認 → `flutter drive --driver=test_driver/integration_test.dart
     --target=integration_test/ui_polish_shots_test.dart -d <iPhone 17 / iOS 26.4 の udid>`
     （**事前に `xcrun simctl uninstall <udid> com.hidefozu.kakeibo`**）→
     `build/qa_screens/polish_1..16.png` を Read で確認
  3. `pubspec.yaml` の +N を1つ上げて `flutter build ipa` → ipa内 CFBundleVersion 検証 →
     `flutter build ios --profile` → `xcrun devicectl device install app --device
     188670B5-1E37-5AB9-A069-557E148BC045 build/ios/iphoneos/Runner.app`
  4. コミット（feat/style + chore: version）＋本書を更新
- **罠**: `flutter drive ... | tail -2` はパイプで進捗が見えず、ハングと区別が付かない。
  **`nohup flutter drive ... > log 2>&1 &` にしてログを追う**のが安全（一度20分ハングして
  kill→再実行で23秒で通った実績あり）。sim は **iPhone 17 / iOS 26.4** を使う
  （26.5 は takeScreenshot が白画像になる既知の罠）。
- **UI判断で迷ったら**: 実装せず実験ビルドでスクショを撮り、現行版と2枚並べて
  `open` でプレビュー表示 → ユーザーに選んでもらう。カテゴリ帯の位置はこれで即決した。

## ✅ 済（2026-08-13）: 指定パレット適用＋入力画面の位置合わせ（未コミット・実機build 40）

**build 2.2.0+40 を実機TI10B1に配備済（profile）。530テスト緑・analyze 0。未コミット。**

### 配色: ユーザー指定のパレットに全面差し替え（theme.dart）

指定値: 背景#FFFCF7 / Surface#FFFFFF / Primary#6EDCC7 / PrimaryLight#DDF7F0 /
PrimaryDark#287F73 / Text#343A3A / TextSub#8B918F / Border#E9E6E1 /
Expense#F07878 / ExpenseLight#FFE5E3 / Income#54C69A /
Accent Yellow#F4C95D・Orange#F4B557・Blue#65AFE0・Purple#8B82E8。

- **役割分担が肝**: Primary #6EDCC7 は白背景の文字として **1.6:1** で読めない。
  → `scheme.primary` には **PrimaryDark #287F73**（文字・枠・アイコン用）を入れ、
  **塗り・CTA・選択状態**に Primary(`kMint`)を使う。
  **CTA＝Primary塗り＋Text Primary文字で 7.0:1**（`filledButtonTheme` /
  `floatingActionButtonTheme` で上書き。カスタムアクセント時は従来どおり seed 由来）。
- 指定に無い派生: `kIncomeSoft`・`kWarnMuted`(#C7605C＝Expenseは文字だと2.7:1)・
  曜日色・`kSubScale`(ミント階調)・`kConfidence*`。
- **タブは選択中だけ PrimaryDark**（`selectedIcon`）。未選択のアクセント色のままだと
  選択中の丸(PrimaryLight)に埋もれて **1.3:1 で消える**実害があった。
- **未対応の計測値（ユーザー判断待ち）**: 一覧の金額 Expense #F07878 は背景上
  **2.68:1**（本文基準4.5未満）。Accent Yellow/Orange のタブアイコンは 1.5〜1.8。
  「低彩度にしない」という指示があるので指定のまま入れてある。締めるなら
  Expense→#D45C5C(3.74)。
- **カレンダーは「ヘッダはベース背景／本体と一覧は白」**。BackupBanner と
  _MonthHeader の下を `Material(color: kCard)` で包む。**Container(color:) だと
  ListTile のインク波紋が消えると Flutter が警告する**ので Material にすること。

### 入力画面: 1品目と複数で位置を揃える

**縦の並びを内訳画面に合わせた**（従来は電卓がカテゴリの上にあり、内訳では下。
「カテゴリを追加」の前後で全部が飛んでいた）。確定した並び:

  支出/収入 → 日付+金額 → 店名 → **[毎月の費用｜カテゴリを追加] ⇄ [内訳＋消費税]**
  → 品目行 → カテゴリ見出し → カテゴリ(グリッド/帯) → 電卓 → 予告帯 → メモ → 保存

- **「カテゴリを追加」を消費税行と同じ位置に置く**のが決め手（ユーザー発案）。押すと
  その場が消費税行に入れ替わるだけなので、品目行から下が動かない。
- 支出/収入トグルは**内訳中も表示**（隠すと位置が飛ぶ）。店名も品目行の上へ移動。
- 残るズレは2品目めの行が増えるぶんだけ（行が増える以上は不可避）。
- **「カテゴリを追加」等を初期表示に**（金額0でも出す）。**代わりに保存できない理由
  (`save-hint`)は「保存」を押した後だけ**表示 = `EntryFormState.saveAttempted` を新設し
  `markSaveAttempted()` で立てる。**保存ボタンは無効化をやめ常に押せる**（無効だと
  押せず理由を出せない）。これで初期表示でも縦が溢れない。
- **サブカテゴリ帯に「完了」ボタン**（`sub-done`→`confirmParentCategory()`）。
  サブカテゴリを選ばず親のまま確定して閉じる。「追加」は
  `entrySubcategoryAddButton`（9ロケール）をツールチップにしたアイコンのみ
  （ラベルを出すと独語などで1行に収まらない）。
  **同じ流れが内訳のカテゴリ帯にもある（そちらは「‹」で戻る）＝未対応**。
- テスト修正: 保存ボタンのonPressed=null前提と「金額0では start-split が出ない」を
  新仕様へ更新（entry_screen_test / split_entry_test）。

## ✅ 済（2026-08-12）: パステル化＋タブ色分け＋「1品目も内訳の1行目として見せる」（未コミット）

**build 2.2.0+37 を実機TI10B1に配備済（profile）。530テスト緑・analyze 0。未コミット。**

### 配色（FB「暗い」「もっとパステルに」→ 3回に分けて比較撮影で決定）

**測って分かったこと**: カレンダー画面の**97%が `kPaper` #F6F5F1**（温かいベージュ）で、
「暗い」の主因は緑ではなく下地だった。参考シートの実測値はラベル≒黒 #0E1B20 /
タイトル #003F2D / アクセント **#72A395** / アイコンのストローク **#50A090**。
**参考のパステルをそのまま使うと白背景で 1.5〜3.1 とコントラストが足りない**
（保存ボタンは白文字）ので、色相を保ったまま締めた値を使っている。

- `kPrimary` #1E6B5A → **#2F8570**（白文字4.46・案A/B/Cを撮って比較しBを採用）
- `kPrimarySoft` #E4EFE9 → **#E8F2EC**
- `kPaper` #F6F5F0 → **#FFFFFF**（純白。F4F7F5/F8FAF9 の小刻みは「まだ暗い」と却下）
- `kLine` → #EDECE6、**`kChrome` #F2F6F4 を新設し `surfaceContainerHighest` を上書き**
  （seed由来の #DFE4E1 が濃く、バックアップ帯・電卓下の帯を暗くしていた）
- **下部タブをタブごとの色に**（FB「灰色じゃなくてパステルカラー」）。
  `kNavMint/kNavAmber/kNavCoral/kNavBlue` を home_shell の各 destination に。
  **ラベル色は NavigationBar の仕様上タブ別にできない**ので `kNavIdle` #5FA491 で共通。
  ラベルも色分けするなら自前タブバーへの差し替えが要る。
- **注意**: 純白背景にしたので「入力欄が背景に同化する」(2026-08-09のFB)が再発しうる。
  現状は白fill＋各所の OutlineInputBorder で枠が出ているが、実機で要確認。

### 1品目でも「内訳の1行目」として見せる（FB「最初から複数カテゴリの状態で1つだけ表示」）

**ユーザー選択＝「1品目は2段グリッド・2品目以上で帯」**（常に帯だと一目で見える
カテゴリが8→4に減り長押し並べ替えも使えない、常にグリッドだと縦が足りず
3日かけて消した「電卓/保存が押される」問題が再発する、という比較の上で）。

- 通常モードに**品目行 `single-item-row`** を追加（`1 [カテゴリチップ]`）。内訳行と
  同じ見た目。**金額は行に出さない**（1品目では必ず総額と同じ＝二重表示。実際に
  テストが「¥500が2つある」と検出した）。長い言語(de)対策で Flexible＋ellipsis。
- **`startSplit` が選択済みカテゴリを1行目に引き継ぐ**ようにした。従来は2行とも空で
  作られ、カテゴリを選んでから内訳に入ると選び直しになっていた（今回の要望の核心）。
- ボタン **「複数のカテゴリを選択」→「カテゴリを追加」**（9ロケール）。アイコンも
  call_split → add。**同名衝突を避けるため `categoryAddTitle`（マスタに新規作成）を
  「新しいカテゴリ」に改称**（9ロケール）。
- **テストの罠**: 行番号バッジ「1」がテンキーの「1」と衝突し `find.text('1')` が
  曖昧に。**digit タップは `find.descendant(of: find.byType(Numpad), ...)` に統一**
  （store_screenshots_test と同じ方式）。entry_recurring_toggle_test を修正済み。
- **未実施**: 「1品目=splitGroupIdなしの通常保存」は現状の作り（1品目は splits==null）
  で自動的に満たされている。将来 splits を常時1行にする作りへ寄せるなら、
  `_saveGroupLines` に1行時の分岐が必須（全記録がレシートまとまりカードになる）。

## ✅ 済（2026-08-11b）: カテゴリアイコンをイラストに差し替え＋タイル意匠（未コミット）

ユーザーが用意したデザインシート（ChatGPT生成・「ナチュラル＆やさしいデザイン
（Bベース＋Cの絵文字）」7列×4行=28個）を適用。**530テスト緑・analyze 0**。
**まだコミットしていない**（version も +35 のまま。次は +36）。

- **アセット**: `assets/category_icons/*.png`（28枚・各120x120px・計292KB）。
  元シートから円1個ぶんを切り出したもので、**円の下地(#F5F8F5)まで画像に含む**。
  描画側で `ClipOval` して使う。切り出しスクリプトは使い捨てのため repo には無い
  （やり直すなら: 行バンド検出 → 各行の中心走査線で円7個を検出 → **行内のy中心の
  中央値と全体の直径中央値で正規化** → 外周からのflood fillで下地色を統一。
  ChatGPT生成画像は列ピッチが206〜248pxとバラつくので等間隔グリッド前提だと壊れる）。
- **対応表**: `lib/core/category_icon_assets.dart` の `_assetBySlug`（slug→ファイル名）。
  slug基準なので9ロケールでも壊れない。**シートは支出向けで収入の絵柄が無く、
  給与/賞与/副収入はシートの余りを充当**（がま口/プレゼント/貯金箱）＝要ユーザー確認。
  未使用の絵柄10個は `kSpareCategoryIcons`（車/クレカ/書籍/買い物/旅行/子ども/ペット/
  税金/冠婚葬祭/サブスク）＝カテゴリを増やすときの在庫。
- **描画**: `lib/app/category_icon.dart` の `CategoryIcon(icon:, slug:, size:)`。
  優先順は **ユーザー設定の絵文字 → slugのイラスト → 既定の絵文字**。絵文字側にも
  同じ色(`kIconCircle`)の円を敷いて粒を揃える。ユーザー作成カテゴリは絵文字のまま。
- **タイル**: `CategoryTileBox`（category_grid.dart）を「白カード＋12R＋hairline枠＋
  26pxの円イラスト＋緑ラベル」に変更。**選択中の見え方（primaryContainer＋2px枠）は
  08-11のFBで詰めた挙動をそのまま維持**（太字だけ追加）。
- **差し替え箇所**: 入力グリッド／内訳の帯／カレンダー日別一覧／カテゴリ管理／
  毎月ハブ／固定費ルール／一括内訳。**文字列に絵文字を埋め込んでいる3箇所**
  （batch_itemize_panel の差分表示2つ・entry_screen のカテゴリ名マップ・
  recurring_rules_page のプルダウン項目）は絵文字のまま＝混在している。
- **確認**: `integration_test/icon_shots_test.dart`（使い捨て）→
  `build/qa_screens/icons_1..6.png`。sim目視のみで**実機未確認**。

## ✅ 済（2026-08-10d）: 残額行の旧ボタン撤去・薄い文字色の修正（build 31）

コミット 3a8a7ce。**version 2.2.0+31・530テスト緑・analyze 0・untranslated 0・
ipa(31)検証済・実機TI10B1にprofile再配備済**。

- **残額行の「＋ カテゴリを追加 ›」を廃止**（旧仕様の残り）。行のどこをタップしても
  割当先になる（背面 GestureDetector `split-line-remainder`）ので導線は失われない。
  カテゴリ確定後のチップ表示は従来どおり（`split-remainder` キーはチップ側に残る）。
  **テスト/ハーネスの残額行タップは `split-line-remainder` に統一**。
  l10n `splitAddCategoryLabel` は未使用になったので9ロケールから削除。
- **薄すぎた文字色を修正**: 罫線トークン kLine（`scheme.outline`）を文字・アイコンに
  使っていた箇所を `onSurfaceVariant`(kMuted) へ。入力/内訳の「カテゴリ」見出しが
  ほぼ見えなかったのが発端（ユーザーFB）。同じ誤用だった一括内訳パネルのラベル群・
  レシート確認の「店舗名」ラベル・内訳行の番号バッジ・行削除✕もまとめて修正。
  **`scheme.outline` は罫線専用。文字は `onSurfaceVariant` を使う**（再発注意）。

## ✅ 済（2026-08-11）: 帯を電卓に重ねる・選択済みカテゴリを濃く（build 35）

コミット 73f6733。**version 2.2.0+35・530テスト緑・analyze 0・ipa(35)検証済・
実機TI10B1にprofile再配備済**。

- **展開した帯を電卓に重ねる**（3品目＋展開で電卓がスクロール送りになるFB）。
  帯は1行ぶんの枠だけ確保し、2行目は `Stack` + `Positioned(top:0)` で電卓の上に
  重ねる（通常モードの内訳チップ浮かせ表示と同じ手）。**開閉でレイアウトが動かない**
  ＝電卓も保存ボタンも不動。開いている間だけ背景(scaffold色)＋枠＋影を敷いて
  電卓が透けないようにしている。`Numpad` はローカル変数に切り出し、分割時は
  Stack の中・通常時は従来位置に置く。
- **選択済みカテゴリのブロックを濃く**: `primaryContainer` 全不透明＋緑枠
  （行の中でカテゴリの塊の範囲が分かりにくい、というFB）。未選択の赤茶枠と対の形。
  入力行・残額行の両方に適用。

## ✅ 済（2026-08-10g）: 残額行にも行番号（build 34）

コミット a74f20b。**version 2.2.0+34・530テスト緑・analyze 0・ipa(34)検証済・
実機TI10B1にprofile再配備済**。

- 残額行だけ番号が無く別物に見える、というFB。**入力行と同じ連番（i+1）**を
  同じ位置・体裁で表示（1品目なら残額行は「2」、＋品目で「3」…と自動繰り上がり）。
  残額行は「カテゴリを選べばその番号の品目になる枠」なので、データ構造上も
  `lines[last]` の連番＝正しい表示。色も入力行と同じ規則（アクティブ=primary）。
- 検討した代替案: (a)「残」等の記号 (b)点線の丸に薄い番号＝仮の番号だと示す
  (c)左端に「残り」ラベルを移動。いずれも「番号列が空く」問題は消えるが、
  ユーザー案（連番）が実装・意味とも一致するので採用。
- `split_entry_test` に残額行の番号アサートを追加（1品目時に `split-lineno-1`＝'2'）。

## ✅ 済（2026-08-10f）: 内訳の「足りない所」を明示（build 33）

コミット c5994fb。**version 2.2.0+33・530テスト緑・analyze 0・untranslated 0・
ipa(33)検証済・実機TI10B1にprofile再配備済**。ユーザーFB3点:

1. **残額行（最終行）にも「カテゴリ未選択」**。前回 旧仕様の「＋ カテゴリを追加」を
   消した結果、最終行だけ何も出ず不自然だったため。入力行と同じ赤茶ピル。
2. **金額が空の行は '—' ではなく「金額未入力」**（新キー `splitAmountEmpty`×9）。
   **黒字**＝入力待ちの案内、**赤茶(kWarnMuted)**＝要対応、と役割で色を分けている。
   保存を押した時だけ出す案もあったが、常時表示のほうが状態が読めるので常時に。
3. **「カテゴリ未選択」を押すとカテゴリ帯が2行に開く**（`splitCatPickerOpen` を
   そのまま可視状態に流用。カテゴリ確定で1行に戻る）。ボタンに見えるのに押しても
   反応が薄い、というFBへの対応で、一度に見えるカテゴリが4→8になる。
   2行は「縦2枚の列を横スクロール」＝読み順が崩れない並べ方。
- 収まり: 1品目なら展開しても電卓は最下段まで見える（polish_15）。3品目＋展開だと
  電卓の最終行が隠れるがスクロールで到達可（polish_14）。カテゴリを選べば1行に戻る。

## ✅ 済（2026-08-10e）: 内訳のカテゴリ帯を電卓の上へ（build 32）

コミット 49f37c6。**version 2.2.0+32・530テスト緑・analyze 0・ipa(32)検証済・
実機TI10B1にprofile再配備済**。

- 2枚のスクショ比較（`current_strip_below.png` / `experiment_strip_above.png`）を
  ユーザーが見て**「こっちの方が自然」→ 上に置く案を採用**。
- 分割中の並び = **行 → 残額行 → カテゴリ帯 → 電卓**（`entry_screen.dart` の
  `if (splitMode) ...[]` に SplitEntryPanel＋見出し＋SplitCategoryStrip をまとめ、
  電卓下のブロックは削除）。通常/一括内訳は従来どおり「電卓 → カテゴリ」なので、
  **帯/グリッドの位置はモードで入れ替わる**（意図的・迷ったら戻さないこと）。
- 行が増えても行リストは固定高（2行窓）でスクロールするので、帯と電卓が下がるのは
  1行ぶんで止まる。3行時も保存ボタンまで収まるのを確認（polish_13）。

## ✅ 済（2026-08-10c）: 内訳の「カテゴリ未選択」表示（build 30）

コミット b05aa73。**version 2.2.0+30・530テスト緑・analyze 0・untranslated 0・
ipa(30)検証済・実機TI10B1にprofile再配備済**。

- 内訳行のカテゴリ未選択が旧仕様の**緑ベタ「＋ カテゴリ」ボタン**のままだったのを、
  **塗りなし・赤茶の文字と枠の「カテゴリ未選択」**に変更（`split-pickcat-$i` の
  タップ挙動＝カテゴリ帯の割当先にする、は不変）。
- **`kWarnMuted` (#A35B4C) を theme.dart に新設**。未選択の行では常時出るため、
  エラー赤 `kExpense` (#B8433A) より彩度を落とした赤茶にする、というユーザー指定。
  今後「常時出る注意喚起」はこのトークンを使う（エラー＝kExpense と使い分け）。
- l10n: `splitAddCategoryChip`（＋ カテゴリ）を廃止し
  `splitCategoryUnselected`（カテゴリ未選択 / No category …）に置換（9ロケール）。
  store_screenshots_test.dart の参照も更新済み。

## ✅ 済（2026-08-10b）: 入力欄の枠を統一・固定費の日の文言（build 29）

コミット a8217e1。**version 2.2.0+29・530テスト緑・analyze 0・untranslated 0・
ipa(29)検証済・実機TI10B1にprofile再配備済**。

- **下線だけだった入力欄を OutlineInputBorder に統一**（つきいちの項目名/絵文字・
  つきいち記録のメモ・カテゴリ追加/改名ダイアログの名前/アイコン）。白背景化で
  「白い箱に下線だけ」が不自然になったため。ダイアログの2欄には間隔12pxを追加。
  ※内訳パネルのインライン店名/メモなど、行内に埋め込む dense な欄は従来どおり
  下線のまま（枠を付けると行が崩れる）。
- **文言（ユーザー指定・9ロケール）**: 固定費フォームの「記録する日」→
  **「繰り返し入力する日」**、月末丸めの説明を
  **「31日で入力した場合は月末日に入力されます。例：2月は28日に入力」**へ。
  文が伸びるので `helperMaxLines` を 3 に。
- QAスクショ: polish_2（新文言＋日メニュー）・polish_4（枠付きのつきいちフォーム）・
  polish_12（カテゴリ追加ダイアログ）。

## ✅ 済（2026-08-10）: 入力欄を白背景に・「追加」を緑ボタンに（build 28）

コミット 0e5c632。**version 2.2.0+28・530テスト緑・analyze 0・ipa(28)検証済・
実機TI10B1にprofile再配備済**。

- **入力欄の白背景**: `theme.dart` に `inputDecorationTheme(filled: true,
  fillColor: kCard)` を追加。「文字を入力できる場所がページ背景(bg)に同化して
  分からない」というFB。全画面の TextField/TextFormField が白ピル・カードと同じ
  白になる（入力画面の店舗名/詳細メモ・固定費の金額/店舗名/メモ・つきいちの
  項目名/絵文字・内訳のインライン店名・各ダイアログ）。
- **CellDropdownField も applyDefaults を明示**。`InputDecorator` は TextField と
  違いテーマ既定を自動では拾わないため、これが無いとフォーム内で
  「テキスト欄=白 / プルダウン=透明」とチグハグになる（同じ見た目に揃えた）。
- **毎月タブの「＋ 追加」を FilledButton に**（緑ベタ＋白文字・compact）。
- QAスクショ再取得: polish_1（緑の追加ボタン）/2/4/6/7 + polish_11（内訳の
  インライン店名が白）。

## ✅ 済（2026-08-09深夜）: つきいちの繰り返し単位を選べるように（build 27）

コミット 4afe15c。**version 2.2.0+27・全530テスト緑・analyze 0・untranslated 0・
ipa(27)検証済・実機TI10B1にprofile再配備済**。

- 直前に「毎月N日」へ一本化したのを見直し、**フォームで単位を選ぶ二本立て**に:
  `[繰り返し: 毎月 | 日ごと ▾]` ＋ 横の値プルダウン（`chore-form-day`=予定日1..31 /
  `chore-form-interval`=間隔1..180、単位ごとに別々のstateで保持するので往復しても
  入力が消えない。復元データが180超なら現在値を選択肢に足す）。
- **schema v8→v9**: `repeat_unit`（既定monthlyDay）と `interval_days`（既定30）を追加。
  **移行は経路別**: v7（N日ごとしか無かった世代）→ everyDays を維持し間隔もそのまま、
  予定日だけ anchor+interval の日で初期化 / v8（毎月N日だけの世代）→ monthlyDay＋
  既定間隔 / v6以前 → 現行定義で新規作成。**「実機のv7データは間隔を失わない」**のが
  この分岐の目的（build 26 を入れた端末＝v8はmonthlyDayになる。実機TI10B1が該当）。
- **backup formatVersion 6→7**: repeatUnit/intervalDays を同梱。v5は everyDays、
  v6は monthlyDay として復元（v5→v6で intervalDays を消さず残し、v6→v7の目印にする）。
- 期日: everyDays=最後にやった日＋間隔（記録なしは anchor＋間隔）/ monthlyDay=従来。
  通知文言・一覧・履歴も単位で出し分け（`choreRepeatText`・
  `choreNotificationBodyInterval`）。
- l10n新規: choreRepeatUnitLabel/Monthly/EveryDays・choreFormIntervalLabel・
  choreIntervalDaysItem・choreIntervalEvery・choreNotificationBodyInterval（×9）。
  choreFormDayLabel は2列に並ぶため「予定日」に短縮。
- QAスクショ追加: polish_8（単位メニュー）/9（日ごと選択時）/10（間隔メニュー）。

## ✅ 済（2026-08-09夜）: ユーザーFB 5件 = つきいち毎月N日化ほか（build 26）

コミット 3e2c6e5（feat）+ 版上げ。**version 2.2.0+26・全518テスト緑・analyze 0・
untranslated 0・ipa(26)検証済・実機TI10B1にprofile配備済**。

1. **つきいちタスク「毎月N日」化**: 間隔日数（N日ごと）を廃止し固定費と同じ
   「毎月N日」に。期日=記録が無ければanchor以降で最初のN日／あれば実施月の
   翌月N日（月末丸め）。**schema v7→v8**（interval_days→day_of_month・既存行は
   anchor+intervalの日を引き継ぐ）・**backup v5→v6**（同規則で移行）。
   フォームは「毎月の予定日」プルダウン（既定=今日の日）・通知文言「毎月N日の予定です」。
2. **共通プルダウン `lib/app/cell_dropdown.dart`**: セルと同じ幅・セルの直下にだけ
   展開（DropdownButtonの画面いっぱいメニューを廃止）・最大5.5行スクロール・
   選択中央スクロール+ハイライト・白背景。適用=ルール編集3つ(カテゴリ/記録する日/
   開始月)・つきいち予定日・入力の記帳日ピル・基準日シート。
   **CellDropdownField**=OutlineInputBorder見た目のフォーム用。
3. **日項目の文言「毎月1日」→「1日」**（新キーdayOfMonthItem×9ロケール。
   一覧の「毎月N日」表記=recurringEveryMonthDayは従来どおり）。
4. **毎月タブの追加ボタン**: ＋アイコン→「＋ 追加」（commonAdd流用）。
5. **収入は「店舗名」→「会社名」**（entryCompanyNameLabel×9・入力/ルール編集とも
   typeで切替）。
- **テストの学び**: cell_dropdownメニューは遅延構築+コンパクト高なので、sim/widget
  テストの項目タップは `scrollUntilVisible(item, scrollable: Scrollable.last)` 必須
  （tsukiichi_shots の pickCellMenuItem ヘルパ参照）。iOSの pageBack() は
  CupertinoBackButton前提で失敗→ find.byType(BackButton) をタップ。
- QAスクショ: build/qa_screens/polish_1..7（＋追加/日メニュー2種/カテゴリメニュー/
  会社名/ピルメニュー）+ tsukiichi_1..8 再生成済み。ハーネス=
  integration_test/ui_polish_shots_test.dart（使い捨て・コミット済）。
- **実機受入に追加**: つきいちフォームの新プルダウン操作感。
  ※build 26 を一度入れた端末（=DBがv8）は既存タスクが「毎月N日」に変換済みなので、
  「N日ごと」に戻したい項目は編集画面で単位を選び直す（v9移行では復元できない）。

## ✅ 済（2026-08-09）: 予告帯の日プルダウン白ピル化 + ipa再生成

- **コミット 9826238**: `entry-recurring-day` を白ピル（surface背景+outlineVariant枠+
  角丸8 = 行メモ・内訳チップと同じ文法）で包み、`selectedItemBuilder`（固定幅20+Center）
  で数字を中央揃え・▾密着・メニュー項目も中央寄せ。
  sim(iPhone 17/iOS 26.4)スクショで帯・メニュー開の両方を目視確認済み
  （build/qa_screens/daypill_*.png）。全509テスト緑・analyze 0。
- ~~ipa 2.2.0(25)~~ → **build 26 に置き換え済み（上記）**。

## 現在地（このセッション終了時点）

- **ブランチ `feature/tsukiichi-merge`**（`feature/recurring` 82a1410 から分岐・main未マージ・未push）。
- **全530テスト緑・analyze 0・gen-l10n untranslated 0**。version **2.2.0+35**。
- **ipa = `build/ios/ipa/kakeibo_app.ipa` 2.2.0(35)**・CFBundleVersion=35検証済・zip -T OK。
  次の機能は **+36** から。schema **v9**・backup **v7**。
- **実機TI10B1にprofileビルド配備済み（2026-08-08）**。※profile側のCFBundleVersionは24表示だが中身は同一コミット。
- **⚠️Xcode採番の罠**: `flutter build ipa` の書き出しが「Manage Version and Build Number」で
  ビルド番号を自動採番することがある（今回2回目のexportで24→25に勝手に+1）。
  **ipa内のCFBundleVersionを毎回検証**し、pubspecは実態に合わせる。次の機能は **+26** から。
- 追加実装（2026-08-08夜・コミット3b139b3）: ①「毎月の費用」の**記帳日を変更**
  （予告帯の「記帳日を変更」→毎月N日ダイアログ。取引日は不変・EntryFormState.recurringDay）
  ②ボタン行の見切れ修正（Row+Spacer+Flexibleのflex均等割り罠→Wrap化。
  「複数のカ…」解消・長い言語は2行折返し）。
- UIモック（承認済み・全案比較の記録つき）: https://claude.ai/code/artifact/7edc8c8b-8beb-4cf5-8a08-c676b077f6bc
- コミット列: 2b49cc3(P1基盤) → b02b0e5(P2毎月タブ) → 27681e3(P3カレンダー統合) → 8e80d96(P4入力トグル) → 9b2f1c2(P5バックアップ+l10n) → 以降 QA/ipa/本書。

## ⚠️ リリース順序の注意（ユーザー判断が必要）

**v2.1.0(build 23) はまだASCで「審査用に追加→提出」されていない**（2026-08-05時点のまま）。
選択肢: (a) 2.1.0を提出→通過後に2.2.0(24)を上げる（2段階） (b) 2.1.0を飛ばして
2.2.0を直接提出（What's New は2.1+2.2の合算に書き直し）。ipa 24 はどちらでも使える。

## v2.2.0 の内容

### 1. つきいちタスク合体（routine-reminder v1 を移植）
- **schema v6→v7**: `chore_tasks` / `chore_records`（日付は CivilDateConverter・FKカスケード）。
- domain: `chore_schedule.dart` 純関数（次回期日=最後にやった日+間隔・buildChoreStatuses/
  choreMonthMarks/buildChorePlans[文言レス]/choreOverdueCount）。**DateOnlyは移植せず
  CivilDate に addDays/differenceInDays/isBefore/isAfter を追加して統一**。
- `ChoreActions`（features/chores/application）: 全ミューテーション→resync（通知全再予約+
  バッジ）。**resyncはFuture読み（`watch().first`はfake asyncでハングする既知の罠）**。
  通知文言はresync時点のロケールで組み立て（ロケール変更でhome_shellが再予約）。
- 通知/バッジ: `data/notifications/`（flutter_local_notifications 22.x named-params API・
  zonedSchedule・上限60件・許可要求は初回記録後の一度だけ）。**providerはNoop既定**
  （throw既定にするとhome_shell起動時resyncで全widgetテストが死ぬ）。bootstrapがiOSのみ
  実物へ差し替え+tz初期化（失敗時UTC続行）。AppDelegateに UNUserNotificationCenter
  delegate 1行（フォアグラウンドバナー用）。
- 新パッケージ: flutter_local_notifications ^22 / timezone ^0.11 / flutter_timezone ^5.1 /
  app_badge_plus ^1.3 / app_settings ^7。

### 2. 「毎月」タブ（4タブ化・管理ハブ）
- `features/monthly/presentation/monthly_hub_screen.dart`。ボトムナビ=カレンダー・毎月・
  サマリ・設定（`_navToShell=[0,4,2,3]`・入力(1)は従来どおりタブ外）。
- 構成: 今月のこれから（家事期日+固定費予定+見込み収支行）→ 固定費・収入
  （ON/OFFスイッチ=update+applyDue・タップで既存 RecurringRuleEditPage・＋新規）→
  つきいちタスク（＋→chore_task_form・タップ→履歴）。
- 設定に「つきいちタスク」タイル（chores-tile）→ 通知時刻・許可状況・アーカイブ復元。

### 3. 統合カレンダー（ゴースト・家事ドット・見込み収支）
- **ゴースト述語**（recurring_schedule.dart `upcomingOccurrencesInMonth`）:
  `isActive && startYm<=ym && (endYm null || ym<=endYm) && ym > (lastGeneratedYm ?? 0)
  && dueDateIn >= today`。**重複排除は watermark のみ**（実取引と突き合わせない）。
  `>= today` が重要: `>` だと日付変更〜resumeの間に当日分が消える。
- **見込み収支** `monthForecast`: 実績差引+基準日までの予定。基準日=設定
  `forecastAnchorDay`（0=月末・1..31）。過去月=非表示。**当月で基準日超過は月末へ
  フォールバック**。ヘッダー3行目（forecast-line）タップ→基準日シート。
- `_dayCell`: 数字→家事ドット（やった=緑43A047/期日=橙FB8C00/超過=赤kExpense・
  keyつき `chore-dot-*-<iso>`）→実績額→予定額グレー（`ghost-amount-<iso>`）。
  **rowHeight 58→62**。凡例は該当月のみ表示。
- 日パネル: 予定行（`ghost-<ruleId>`・タップでルール編集）+ 家事行
  （chore_day_section: 記録✓・期日行・今日のみ超過行+やったボタン）。
  **空状態判定は3レーン全部空のとき**。「やった」は今日のパネルのみ
  （未来は記録不可・過去への遡り記録は履歴の日付編集で代替=既知の簡略化）。

### 4. 入力画面「毎月の費用/収入」トグル（単体登録専用）
- 「複数のカテゴリを選択」の左に `entry-recurring-btn`（label=typeで毎月の費用/収入）。
  ON=緑+予告帯+保存文言「保存（＋毎月の費用に登録）」。
- 可視条件: `!split && !batch && mode==create && replacesTxIds==null && amount>0`。
  内訳/一括開始でOFFリセット・保存後リセット。
- ルール生成: `lastGeneratedYm = maxYm(ymOf(date), prevYm(ymOf(today)))` + 保存直後
  applyDue。**過去日付入力でも経過月をさかのぼって多重起票しない**（当月期日超過分
  のみ1件即起票）。
- **fix**: `DriftRecurringRuleRepository.add` が entity の lastGeneratedYm を落としていた
  （v2.1.0では渡すケースが無く潜在。落とすと当月二重起票）。

### 5. バックアップ formatVersion 4→5
- choreTasks/choreRecords 同梱・v4以前は空補完・厳格検証・復元は
  txs→rules→choreRecords→choreTasks→cats の順削除+逐語ID復元+件数アサート。
- **復元後に chore resync**（backup_controller.restoreFrom）。
- prefs（通知時刻・permissionAsked・forecastAnchorDay）はバックアップ対象外（従来どおり）。

### 6. l10n
- 新規58キー×9ロケール（ja/en は各フェーズで、de/es/fr/it/ko/pt/zh は最終フェーズで充填）。
- **nullable-getter:false のため新キーは追加フェーズで最低 en+ja が必須**（テストはja固定）。

## sim目視QA（2026-08-08 実施済み・全9枚検品パス）

- `integration_test/tsukiichi_shots_test.dart` → build/qa_screens/tsukiichi_*.png（9枚）。
  毎月タブ空/充填・統合カレンダー（ゴースト額28万/8.5万・家事ドット・凡例）・
  日パネル予定行・基準日シート・8/25時点切替（+28万）・入力トグルON
  （帯+保存文言）・保存後（毎月タブに食費ルール追加・見込み+¥278,520）を確認。
- **QAハーネスの罠**: ①iPhone 17 Pro Max(iOS 26.5)simは takeScreenshot が白画像/
  同一フレーム化する → **iPhone 17(iOS 26.4)simで撮る**（convertFlutterSurfaceToImage
  は全部同一フレームになり逆効果） ②`.last`付きファインダは空一致で Bad state →
  scrollUntilVisible には素のファインダ ③実配線の許可ダイアログで createTask が
  awaitブロック → prefs `chorePermissionAsked: true` を事前セット ④シード名は
  「給与」（「給料」ではない）。
- ポリッシュ候補（未対応）: 入力画面でトグルと並ぶと「複数のカテゴリを選択」が
  「複数のカ…」に省略される（ja）。文言短縮 or レイアウト調整の余地。

## テストで踏んだ罠（再発注意・v2.1.0からの追加分）

- 小数通貨（EUR等）のテンキーは `np-00` が `np-dot` に変わる → deテストは数字キーで入力。
- `+¥0` 等の金額アサートは「差引 +¥0」と衝突する → 見込み行は文字列全体でアサート。
- riverpod 3→2.6 移植: `legacy.dart` import削除・`.value ?? []` → `.valueOrNull ?? []`
  （2.6の `.value` はエラー状態でthrowする）。
- `date ?? ref.read(provider)` の型推論が稀にnullableへ倒れる → 明示型注釈で解決。

## 残作業

1. **ユーザー**: リリース順序の判断（上記⚠️）→ Transporterでipaアップロード→TestFlight。
2. **実機受入**（sim では検証不能なもの）: 通知の実発火（毎朝9時・期日日）/
   アイコンバッジ数 / 許可ダイアログ（初回記録直後の一度だけ）/ 通知タップ起動 /
   フォアグラウンドバナー / 設定「つきいちタスク」の許可状況表示。
3. 審査通過後: feature/tsukiichi-merge → main マージ（ユーザー承認後）→
   `! git -C ~/kakeibo-app push`（push はプロジェクト設定で deny。Claudeはコミットまで）。

## スコープ外（後続候補）

- 記帳タイミング「月初にまとめて差し引き」オプション（モック2-C・設計はルール別
  記帳タイミングとして温存）/ サマリ画面への予定反映 / 過去日への「やった」直接記録
  （record_sheet相当）/ しりとり…ではなくシードカテゴリ改名（「家賃」はユーザーが
  アプリ内で改名する運用）。

## 動かし方

- 全テスト: `flutter test`（508本）/ analyze: `flutter analyze` / l10n: `flutter gen-l10n`
- codegen: `dart run build_runner build --delete-conflicting-outputs`
- sim目視QA: `xcrun simctl uninstall <sim> com.hidefozu.kakeibo` →
  `flutter drive --driver=test_driver/integration_test.dart --target=integration_test/tsukiichi_shots_test.dart -d <sim>`
  （出力: build/qa_screens/tsukiichi_*.png・9枚）
- ipa: `flutter build ipa` → unzip Info.plist で CFBundleVersion=24 検証 + `zip -T`
